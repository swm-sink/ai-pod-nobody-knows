# Minimal Python dependencies for test scripts only
# This is NOT a Python application - it's Native Claude Code

# For test scripts in .claude/scripts/
python-dotenv==1.0.1    # Load .env file
requests==2.32.3        # API testing

# Note: The MCP servers have their own dependencies:
# - ElevenLabs MCP: Installed via pip in .claude/mcp-servers/elevenlabs-mcp/
# - Perplexity MCP: Node.js based in .claude/mcp-servers/perplexity-mcp/
